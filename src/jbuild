(jbuild_version 1)

(executables
 ((names        (encoder))
  (public_names (encoder))
  (libraries (batteries dolog parmap cpm minicli camlzip bitv oc45 orandforest
              bst parany dbm cryptokit orsvm_e1071 ocamlgraph ptmap oranger))))
